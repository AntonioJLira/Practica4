Cosas a tener en cuenta:

Se tendra que tener un fichero llamado profesores.txt con el numero credencial del profesor, la contraseña y el tipo de profesor que es.Dejo uno preparado para poder loogearse (al haber trabajado con ficheros de texto y no con binario) solo bastaria con abrirlo y mirarlo.

Al abrir el fichero en html se tendra que usar el explorador firefox, para usarlo en google simplemente seria cambiar la instruccion de system y poner google en vez de firefox en el char que lee.

El DNI puede tener letra o no , el programa lee el string del DNI , de esta manera se pueden implementar de las dos maneras, en nigun momento comprueba si la letra coincide con los numeros.


A la hora de introducir el fichero a leer no hay que poner extension , ya esta por defecto en .txt simplemente con poner el nombre del fichero bastaria.


